import { CustomersortPipe } from './customersort.pipe';

describe('CustomersortPipe', () => {
  it('create an instance', () => {
    const pipe = new CustomersortPipe();
    expect(pipe).toBeTruthy();
  });
});
